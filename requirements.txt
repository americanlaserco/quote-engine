flask>=3.0          # Web framework
gunicorn>=21.2      # Production WSGI server (Render uses this)
ezdxf>=0.18         # DXF parsing
pymupdf>=1.23       # PDF / AI vector reading (native, no external tools)
pyserial>=3.5       # Serial communication (Ruida USB) — shop-local only
click>=8.0          # CLI framework
matplotlib>=3.5     # DXF rendering
groq>=0.4           # Groq API client
scikit-learn>=1.3   # Da